import FakedPromise from './faked-promise';
import OperationStack from './operation-stack';
import { Operator } from './operator-kit';
import { reduce, map, filter, spawn } from './operators';

export { FakedPromise, OperationStack, Operator, reduce, map, filter, spawn };
